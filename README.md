# bloomland

# Rules.
1. Instead of specifying a player, it would be faster to get it from the id.
 * Example: ``$player->getId()`` gets the id and then searches for it `$worldManager->findEntity($id)``.
